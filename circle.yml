machine:
  python:
      version: 2.7.3
  services:
    - docker

test:
  override:
    - docker run --rm rh389/docker-test:$CIRCLE_SHA1 bin/phpspec --format=junit

dependencies:
  pre:
    - pip install awscli
    - composer install --prefer-dist
    - docker build -t rh389/docker-test:$CIRCLE_SHA1 .
